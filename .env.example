# ---------------------------------------------------------------------------
# TinaCMS — Tina Cloud credentials
# ---------------------------------------------------------------------------
# Get these from https://app.tina.io → your project → Configuration
# Leave blank to run TinaCMS in local-only mode (no cloud sync).

TINA_CLIENT_ID=
TINA_TOKEN=

# ---------------------------------------------------------------------------
# Git branch (set automatically by Vercel / GitHub CI — no need to edit)
# ---------------------------------------------------------------------------
# GITHUB_BRANCH=main
